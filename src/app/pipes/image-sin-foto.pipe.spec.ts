import { ImageSinFotoPipe } from './image-sin-foto.pipe';

describe('ImageSinFotoPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSinFotoPipe();
    expect(pipe).toBeTruthy();
  });
});
